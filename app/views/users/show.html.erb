<h1><%= t('views.common.title_show', name: User.model_name.human) %></h1>

<p>
  <strong><%= User.human_attribute_name(:email) %>:</strong>
  <%= @user.email %>
</p>

<p>
  <strong><%= User.human_attribute_name(:name) %>:</strong>
  <%= @user.name %>
</p>

<p>
  <strong><%= User.human_attribute_name(:postal_code) %>:</strong>
  <%= @user.postal_code %>
</p>

<p>
  <strong><%= User.human_attribute_name(:address) %>:</strong>
  <%= @user.address %>
</p>

<p>
  <strong><%= User.human_attribute_name(:self_introduction) %>:</strong>
  <%= @user.self_introduction %>
</p>

<p>
  <strong><%= User.human_attribute_name(:avatar) %>:</strong>
  <% if @user.avatar.attached? %>
    <br>
    <%= image_tag @user.avatar.variant(resize_to_fill: [150, 150])%>
  <% end %>
</p>

<p>
  <strong><%= t('follow.followee') %>:</strong>
  <%= @user.followees.count %> : <%= link_to t('views.common.index', name: nil), followees_user_path(@user.id) %>
</p>

<p>
  <strong><%= t('follow.follower') %>:</strong>
  <%= @user.followers.count %> : <%= link_to t('views.common.index', name: nil), followers_user_path(@user.id) %>
</p>

<% if current_user == @user %>
  <%= link_to t('views.common.edit'), edit_user_registration_path %> |
<% else %>
  <% if current_user.follow?(@user) %>

    <%= form_with model: current_user.following_relationships.find_by(followee_id: @user.id),html: { method: :delete } do |f| %>
      <%= f.submit "Unfollow", class: "btn" %>
    <% end %>
  
  <% else %>
  
    <%= form_with model: current_user.following_relationships.build do |f| %>
      <div><%= f.hidden_field :followee_id, value: @user.id %></div>
      <%= f.submit "Follow", class: "btn btn-primary" %>
    <% end %>
  
  <% end %>
<% end %>
<%= link_to t('views.common.back'), users_path %>
